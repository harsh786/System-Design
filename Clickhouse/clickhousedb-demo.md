Create the project for the demo of clichouse db
1. Implement the functionality to ingest the logs and http tracing .
2. Inside the demo apllication also create api that have logs and http trace via open telemntery OTLEL. Save both logs and http trace of api's in clickhouse db
Create reacts js ui to serach the logs and http trace with filters like trace id, service name, environment name.
I want to create whole observability UI via clickhouse. 
Create the application that it should be extendanble for more functionality like new relic.